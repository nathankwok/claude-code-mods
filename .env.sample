ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
ELEVENLABS_API_KEY=
ENGINEER_NAME=
FIRECRAWL_API_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=
OLLAMA_HOST=
OPENAI_API_KEY=

# =========================
# Bitbucket API Configuration
# =========================
# Configuration for hybrid Bitbucket API integration
# Copy to .env and fill in your credentials

# Bitbucket Server URL
# For Bitbucket Cloud, use: https://api.bitbucket.org
# For Bitbucket Server, use: https://your-server.com/bitbucket
BITBUCKET_URL=https://api.bitbucket.org

# Bitbucket Authentication
# Your Bitbucket username
BITBUCKET_USERNAME=your_username

# PREFERRED: API Token (recommended)
# Generate at: https://bitbucket.org/account/settings/personal-access-tokens/
# Required permissions: Repositories (Read, Write), Pull requests (Read, Write)
BITBUCKET_API_TOKEN=your_api_token_here

# FALLBACK: App Password (deprecated but still supported)
# Generate at: https://bitbucket.org/account/settings/app-passwords/
# Used if BITBUCKET_API_TOKEN is not provided
# Note: App passwords are being deprecated by Atlassian
BITBUCKET_APP_PASSWORD=your_app_password_here

# Authentication Priority:
# 1. BITBUCKET_API_TOKEN (if provided)
# 2. BITBUCKET_APP_PASSWORD (fallback)

# Workspace name (optional - can be auto-detected from git remote)
# This is the workspace/organization name in Bitbucket URLs
BITBUCKET_WORKSPACE=your_workspace_name

# Optional Configuration
# =====================

# Logging level for Bitbucket operations
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Enable response caching to reduce API calls
CACHE_ENABLED=true

# Cache TTL in seconds (5 minutes default)
CACHE_TTL=300

# Request timeout in seconds
REQUEST_TIMEOUT=30

# Maximum retries for failed requests
MAX_RETRIES=3

# Retry backoff factor (exponential backoff: retry_delay = backoff_factor * (2 ** retry_count))
RETRY_BACKOFF_FACTOR=1

# =========================
# Jira Configuration (MCP)
# =========================
# 
# Jira operations are handled by the Atlassian MCP server.
# No Jira credentials are needed in this .env file.
# Configure Jira access via the MCP server settings.